import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Header from "./components/Header";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Abhay Kumar Gupta",
  description: "Portfolio website of Abhay Kumar Gupta — front-end developer skilled in React, Next.js, and UI/UX design.",
  keywords: ["Abhay Kumar Gupta", "Portfolio", "Frontend Developer", "React", "Next.js", "Tailwind CSS"],
  authors: [{ name: "Abhay Kumar Gupta" }],
};


export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <Header/>
        {children}
      </body>
    </html>
  );
}
